node_modules/
dist/
.astro/

# Docs-test harness local overrides (scripts/docs-test/.env.example is tracked).
scripts/docs-test/.env
# Cycle 4 (docs-test.sh): remembers the last `up --profile ...` set so
# `docs-test.sh reset` can replay it.
scripts/docs-test/.last-profiles
