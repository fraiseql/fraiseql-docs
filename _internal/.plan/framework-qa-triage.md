# Framework QA triage register

Bugs filed against `~/code/fraiseql` during phases 00–08. Triaged in phase 09 (`personas.md § Framework Bug-Fixer`).

Format: one row per bug. Update the **Triage** column in phase 09; do not edit other columns retroactively.

| ID | Filed in phase / cycle | Issue URL | Severity (blocker / regression / qol) | Affects page(s) | Repro script | Workaround in docs? | Triage (fix-now / accept-limitation / wrong-as-filed) | Resolution PR |
|----|------------------------|-----------|---------------------------------------|-----------------|--------------|--------------------|-------------------------------------------------------|---------------|
| FW-1 | Phase 00 / Cycle 3 | https://github.com/fraiseql/fraiseql/issues/326 | qol | (future) `concepts/storage`, `guides/storage-backends` | `scripts/docs-test/lib/storage-smoke.sh` exercises sidecars; the FraiseQL server cannot reach Azurite/fake-gcs through configuration | Pending — overlays `configs/overlays/storage-azure.toml` and `storage-gcs.toml` document the limitation in their leading comment block; Cycle 5's smoke covers only the S3 backend end-to-end | _(open)_ | _(open)_ |
| FW-2 | Phase 00 / Cycle 5 | https://github.com/fraiseql/fraiseql/issues/327 | regression-or-doc-bug | `getting-started/quickstart.mdx` (per-DB tabs in Step 4 and Step 2 view definitions) | `scripts/docs-test/pages/_smoke.docs-test.sh postgres` proves PG works end-to-end; MySQL/SQLite/MSSQL iterations prove the page's per-DB SQL is correct against the real DB but cannot route through `fraiseql-server` because the binary hardcodes `PostgresAdapter` (`crates/fraiseql-server/src/main.rs:L240-L260`) | Pending — Phase 02 IA decides whether to (a) wire multi-adapter dispatch in framework, or (b) reduce the quickstart to single-DB until support lands. Smoke covers PG end-to-end; other DBs covered at the SQL level so the page's per-DB SQL is verified correct | _(open)_ | _(open)_ |
| FW-3 | Phase 03 / Cycle 1 (Writer-RED) | https://github.com/fraiseql/fraiseql/issues/330 | regression | `building/multi-tenancy` | RED-evidence transcripts `_internal/.plan/red-evidence/phase-03-cycle-01-unregistered-tenant.transcript` and `_internal/.plan/red-evidence/phase-03-cycle-01-two-tenant.transcript` prove the runtime registry/factory/domain/audit hooks are not wired by `build_app_state()`; admin tenant REST routes 404 unconditionally; explicit-deny-on-unregistered tenant is unreachable through the binary | Page must pivot to library-API framing (host binary wraps `fraiseql-server` and calls `with_tenant_registry`/`with_tenant_executor_factory`/`with_domain_registry`/`with_tenant_audit_log`) OR lead with `## Known issues` linking #330 | _(open)_ | _(open)_ |
| FW-4 | Phase 03 / Cycle 1 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/331 | regression | `building/multi-tenancy` (subscription dispatch caveat) | `scripts/docs-test/bugs/multi-tenancy.bug-1.sh` confirms `routes/subscriptions.rs:L183` calls `TenantKeyResolver::resolve(None, &headers, None, false)` — JWT tenant_id dropped, DomainRegistry unreachable, strict mode forced off | Pending — page must state the WebSocket subscription path silently drops JWT-derived tenant context and disables strict validation at this SHA. Cross-link to #331 | _(open)_ | _(open)_ |
| FW-5 | Phase 03 / Cycle 1 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/332 | regression | `building/multi-tenancy` (suspend/resume lifecycle section) | `scripts/docs-test/bugs/multi-tenancy.bug-2.sh` confirms `routes/graphql/handler.rs:L577-L583` maps every `executor_for_tenant` error to `ErrorCode::Forbidden`; the registry's `retry_after = Some(60)` payload is discarded; suspended tenants reach the client as HTTP 403 with no `Retry-After` header | Pending — page's "Suspend / resume lifecycle" section must NOT promise 503 + `Retry-After` against the off-the-shelf binary at this SHA; library-API path honours the contract. Cross-link to #332 | _(open)_ | _(open)_ |
| FW-6 | Phase 03 / Cycle 1 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/333 | qol | `building/multi-tenancy` (tenant-key naming guidance) | `scripts/docs-test/bugs/multi-tenancy.bug-3.sh` proves the header validator (`tenant_key.rs:L108-L122`, alphabet `[a-zA-Z0-9_-]`, length cap 128) and schema-mode validator (`schema_isolation.rs:L31-L40`, alphabet `[a-zA-Z0-9_]`, length cap 56 usable chars after the `tenant_` prefix) disagree on hyphen and length | Pending — page must recommend the tightest intersection (`[a-zA-Z0-9_]`, ≤56 chars) when schema-mode is enabled, and warn that hyphens in tenant keys silently break schema-mode provisioning. Cross-link to #333 | _(open)_ | _(open)_ |
| FW-7 | Phase 03 / Cycle 2 (Writer-RED) | https://github.com/fraiseql/fraiseql/issues/334 | regression | `features/file-storage` | RED-evidence transcripts `_internal/.plan/red-evidence/phase-03-cycle-02-stale-files-toml.transcript` and `_internal/.plan/red-evidence/phase-03-cycle-02-storage-local.transcript` prove the binary loads `ServerConfig` (`server_config/mod.rs:L41`) which has no `storage` field; the canonical `[storage.<name>]` HashMap lives on `RuntimeConfig` (`config/mod.rs:L73-L74`) which the binary never instantiates. `Server::with_storage(backend)` (`server/builder.rs:L583-L588`) is the only wiring path; the binary never calls it, so `/storage/v1/object/*`, `/storage/v1/list/*`, and `/storage/v1/presign/*` all 404 even when the TOML carries a structurally-correct `[storage.<name>]` block | Page must pivot to library-API framing (host binary calls `Server::with_storage(create_backend(&config))` or wires `fraiseql_storage::StorageState` via the subsystems builder), OR lead with `## Known issues` linking #334. Same structural shape as FW-3 #330 — Cycle 1 chose library-API framing. | _(open)_ | _(open)_ |
| FW-8 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/335 | **security (critical)** | `features/file-storage` (presigned-URL section) | `scripts/docs-test/bugs/file-storage.bug-1.sh` confirms `presign_handler` in `crates/fraiseql-storage/src/routes/mod.rs:L373-L434` lacks `Option<Extension<StorageUser>>`, performs no `state.rls.can_*` call, and performs no `state.metadata.get` call. Peer handlers (put/get/delete/list) have 4 `state.rls.can_*` calls — divergence is real. Any anonymous client can presign GET/PUT for any bucket+key | Pending — page MUST warn that the presign endpoint is unauthenticated and the issued URL bypasses every server-side check. Page MUST recommend host-binary middleware that gates `/storage/v1/presign/*` with auth, or deploys the storage routes behind a separate auth layer. Cross-link to #335 | _(open)_ | _(open)_ |
| FW-9 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/336 | regression (data integrity) | `features/file-storage` (bucket-isolation guarantee) | `scripts/docs-test/bugs/file-storage.bug-2.sh` confirms every `state.backend.{upload,download,delete,presign_put,presign_get}` call in `routes/mod.rs` forwards only the path `key` — never `bucket_name`. The metadata table enforces `UNIQUE(bucket, key)` but the backend treats `(A, x)` and `(B, x)` as the same object. Cross-bucket key collisions silently overwrite content; metadata for bucket A's key K can describe bucket B's K bytes after a collision | Pending — page MUST warn that the modern routes do NOT translate bucket names into backend prefixes. Recommend either one logical bucket per backend bucket, or client-side prefix scheme. Cross-link to #336 | _(open)_ | _(open)_ |
| FW-10 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/337 | regression (security) | `features/file-storage` (PublicRead bucket section) | `scripts/docs-test/bugs/file-storage.bug-3.sh` confirms `get_handler` returns user-uploaded content with attacker-controlled `Content-Type`, no `X-Content-Type-Options: nosniff`, no `Content-Disposition: attachment`, no magic-byte verification, and `Cache-Control: public, max-age=3600` (cacheable XSS). `BucketConfig::allowed_mime_types` is `Option<Vec<String>>` with no default — operators must opt in to any allowlist | Pending — page MUST recommend operator-side enforcement (reverse-proxy nosniff + Content-Disposition: attachment, OR strict per-bucket allowed_mime_types). Page MUST NOT mention the hallucinated `validate_magic_bytes` field from the stale page. Cross-link to #337 | _(open)_ | _(open)_ |
| FW-11 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/338 | regression (qol + DoS amplifier) | `features/file-storage` (size-limit configuration section) | `scripts/docs-test/bugs/file-storage.bug-4.sh` confirms `default_max_request_body_bytes() = 1_048_576` (1 MiB) applied globally via `DefaultBodyLimit::max` at the app layer. Legacy `DEFAULT_MAX_UPLOAD_BYTES = 100 MiB` and modern `bucket.max_object_bytes` (unlimited default) are both unreachable until the operator raises the global cap — which then exposes every other route to the elevated limit | Pending — page MUST document the `max_request_body_bytes` ↔ storage upload-size interaction. Page SHOULD recommend deploying storage on a separate server instance for very large uploads, OR using presigned PUT URLs to bypass the server-side body cap. Cross-link to #338 | _(open)_ | _(open)_ |
| FW-12 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/339 | regression (information disclosure) | `features/file-storage` (list endpoint section) | `scripts/docs-test/bugs/file-storage.bug-5.sh` confirms `metadata::list` at `crates/fraiseql-storage/src/metadata/mod.rs:L166-L182` interpolates client-supplied `prefix` into a LIKE pattern via `format!("{pfx}%")` with no ESCAPE clause and no pre-bind `replace("%", ...)` defence. Client passes `prefix=%` or `prefix=%admin%` to enumerate the full bucket / scan for substring matches | Pending — page MUST describe `prefix` as a LIKE pattern (the actual behaviour) until fix lands, OR page MUST warn that PublicRead buckets are enumerable. Cross-link to #339 | _(open)_ | _(open)_ |

---

## Phase 09 triage threshold (G3)

The severity threshold above which phase 09 must close all items before phase 10 starts is **human gate G3**. Default proposal (subject to G3 confirmation): **blocker** and **regression** must be closed-by-fix or closed-as-wrong. **qol** may remain open as accepted-limitation with a `## Known issues` block on the affected page.

When phase 09 opens, the Bug-Finder persona writes a G3 proposal to `handoff.md` and stops for human input.
