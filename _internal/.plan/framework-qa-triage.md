# Framework QA triage register

Bugs filed against `~/code/fraiseql` during phases 00–08. Triaged in phase 09 (`personas.md § Framework Bug-Fixer`).

Format: one row per bug. Update the **Triage** column in phase 09; do not edit other columns retroactively.

| ID | Filed in phase / cycle | Issue URL | Severity (blocker / regression / qol) | Affects page(s) | Repro script | Workaround in docs? | Triage (fix-now / accept-limitation / wrong-as-filed) | Resolution PR |
|----|------------------------|-----------|---------------------------------------|-----------------|--------------|--------------------|-------------------------------------------------------|---------------|
| FW-1 | Phase 00 / Cycle 3 | https://github.com/fraiseql/fraiseql/issues/326 | qol | (future) `concepts/storage`, `guides/storage-backends` | `scripts/docs-test/lib/storage-smoke.sh` exercises sidecars; the FraiseQL server cannot reach Azurite/fake-gcs through configuration | Pending — overlays `configs/overlays/storage-azure.toml` and `storage-gcs.toml` document the limitation in their leading comment block; Cycle 5's smoke covers only the S3 backend end-to-end | _(open)_ | _(open)_ |
| FW-2 | Phase 00 / Cycle 5 | https://github.com/fraiseql/fraiseql/issues/327 | regression-or-doc-bug | `getting-started/quickstart.mdx` (per-DB tabs in Step 4 and Step 2 view definitions) | `scripts/docs-test/pages/_smoke.docs-test.sh postgres` proves PG works end-to-end; MySQL/SQLite/MSSQL iterations prove the page's per-DB SQL is correct against the real DB but cannot route through `fraiseql-server` because the binary hardcodes `PostgresAdapter` (`crates/fraiseql-server/src/main.rs:L240-L260`) | Pending — Phase 02 IA decides whether to (a) wire multi-adapter dispatch in framework, or (b) reduce the quickstart to single-DB until support lands. Smoke covers PG end-to-end; other DBs covered at the SQL level so the page's per-DB SQL is verified correct | _(open)_ | _(open)_ |
| FW-3 | Phase 03 / Cycle 1 (Writer-RED) | https://github.com/fraiseql/fraiseql/issues/330 | regression | `building/multi-tenancy` | RED-evidence transcripts `_internal/.plan/red-evidence/phase-03-cycle-01-unregistered-tenant.transcript` and `_internal/.plan/red-evidence/phase-03-cycle-01-two-tenant.transcript` prove the runtime registry/factory/domain/audit hooks are not wired by `build_app_state()`; admin tenant REST routes 404 unconditionally; explicit-deny-on-unregistered tenant is unreachable through the binary | Page must pivot to library-API framing (host binary wraps `fraiseql-server` and calls `with_tenant_registry`/`with_tenant_executor_factory`/`with_domain_registry`/`with_tenant_audit_log`) OR lead with `## Known issues` linking #330 | _(open)_ | _(open)_ |
| FW-4 | Phase 03 / Cycle 1 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/331 | regression | `building/multi-tenancy` (subscription dispatch caveat) | `scripts/docs-test/bugs/multi-tenancy.bug-1.sh` confirms `routes/subscriptions.rs:L183` calls `TenantKeyResolver::resolve(None, &headers, None, false)` — JWT tenant_id dropped, DomainRegistry unreachable, strict mode forced off | Pending — page must state the WebSocket subscription path silently drops JWT-derived tenant context and disables strict validation at this SHA. Cross-link to #331 | _(open)_ | _(open)_ |
| FW-5 | Phase 03 / Cycle 1 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/332 | regression | `building/multi-tenancy` (suspend/resume lifecycle section) | `scripts/docs-test/bugs/multi-tenancy.bug-2.sh` confirms `routes/graphql/handler.rs:L577-L583` maps every `executor_for_tenant` error to `ErrorCode::Forbidden`; the registry's `retry_after = Some(60)` payload is discarded; suspended tenants reach the client as HTTP 403 with no `Retry-After` header | Pending — page's "Suspend / resume lifecycle" section must NOT promise 503 + `Retry-After` against the off-the-shelf binary at this SHA; library-API path honours the contract. Cross-link to #332 | _(open)_ | _(open)_ |
| FW-6 | Phase 03 / Cycle 1 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/333 | qol | `building/multi-tenancy` (tenant-key naming guidance) | `scripts/docs-test/bugs/multi-tenancy.bug-3.sh` proves the header validator (`tenant_key.rs:L108-L122`, alphabet `[a-zA-Z0-9_-]`, length cap 128) and schema-mode validator (`schema_isolation.rs:L31-L40`, alphabet `[a-zA-Z0-9_]`, length cap 56 usable chars after the `tenant_` prefix) disagree on hyphen and length | Pending — page must recommend the tightest intersection (`[a-zA-Z0-9_]`, ≤56 chars) when schema-mode is enabled, and warn that hyphens in tenant keys silently break schema-mode provisioning. Cross-link to #333 | _(open)_ | _(open)_ |
| FW-7 | Phase 03 / Cycle 2 (Writer-RED) | https://github.com/fraiseql/fraiseql/issues/334 | regression | `features/file-storage` | RED-evidence transcripts `_internal/.plan/red-evidence/phase-03-cycle-02-stale-files-toml.transcript` and `_internal/.plan/red-evidence/phase-03-cycle-02-storage-local.transcript` prove the binary loads `ServerConfig` (`server_config/mod.rs:L41`) which has no `storage` field; the canonical `[storage.<name>]` HashMap lives on `RuntimeConfig` (`config/mod.rs:L73-L74`) which the binary never instantiates. `Server::with_storage(backend)` (`server/builder.rs:L583-L588`) is the only wiring path; the binary never calls it, so `/storage/v1/object/*`, `/storage/v1/list/*`, and `/storage/v1/presign/*` all 404 even when the TOML carries a structurally-correct `[storage.<name>]` block | Page must pivot to library-API framing (host binary calls `Server::with_storage(create_backend(&config))` or wires `fraiseql_storage::StorageState` via the subsystems builder), OR lead with `## Known issues` linking #334. Same structural shape as FW-3 #330 — Cycle 1 chose library-API framing. | _(open)_ | _(open)_ |
| FW-8 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/335 | **security (critical)** | `features/file-storage` (presigned-URL section) | `scripts/docs-test/bugs/file-storage.bug-1.sh` confirms `presign_handler` in `crates/fraiseql-storage/src/routes/mod.rs:L373-L434` lacks `Option<Extension<StorageUser>>`, performs no `state.rls.can_*` call, and performs no `state.metadata.get` call. Peer handlers (put/get/delete/list) have 4 `state.rls.can_*` calls — divergence is real. Any anonymous client can presign GET/PUT for any bucket+key | Pending — page MUST warn that the presign endpoint is unauthenticated and the issued URL bypasses every server-side check. Page MUST recommend host-binary middleware that gates `/storage/v1/presign/*` with auth, or deploys the storage routes behind a separate auth layer. Cross-link to #335 | _(open)_ | _(open)_ |
| FW-9 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/336 | regression (data integrity) | `features/file-storage` (bucket-isolation guarantee) | `scripts/docs-test/bugs/file-storage.bug-2.sh` confirms every `state.backend.{upload,download,delete,presign_put,presign_get}` call in `routes/mod.rs` forwards only the path `key` — never `bucket_name`. The metadata table enforces `UNIQUE(bucket, key)` but the backend treats `(A, x)` and `(B, x)` as the same object. Cross-bucket key collisions silently overwrite content; metadata for bucket A's key K can describe bucket B's K bytes after a collision | Pending — page MUST warn that the modern routes do NOT translate bucket names into backend prefixes. Recommend either one logical bucket per backend bucket, or client-side prefix scheme. Cross-link to #336 | _(open)_ | _(open)_ |
| FW-10 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/337 | regression (security) | `features/file-storage` (PublicRead bucket section) | `scripts/docs-test/bugs/file-storage.bug-3.sh` confirms `get_handler` returns user-uploaded content with attacker-controlled `Content-Type`, no `X-Content-Type-Options: nosniff`, no `Content-Disposition: attachment`, no magic-byte verification, and `Cache-Control: public, max-age=3600` (cacheable XSS). `BucketConfig::allowed_mime_types` is `Option<Vec<String>>` with no default — operators must opt in to any allowlist | Pending — page MUST recommend operator-side enforcement (reverse-proxy nosniff + Content-Disposition: attachment, OR strict per-bucket allowed_mime_types). Page MUST NOT mention the hallucinated `validate_magic_bytes` field from the stale page. Cross-link to #337 | _(open)_ | _(open)_ |
| FW-11 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/338 | regression (qol + DoS amplifier) | `features/file-storage` (size-limit configuration section) | `scripts/docs-test/bugs/file-storage.bug-4.sh` confirms `default_max_request_body_bytes() = 1_048_576` (1 MiB) applied globally via `DefaultBodyLimit::max` at the app layer. Legacy `DEFAULT_MAX_UPLOAD_BYTES = 100 MiB` and modern `bucket.max_object_bytes` (unlimited default) are both unreachable until the operator raises the global cap — which then exposes every other route to the elevated limit | Pending — page MUST document the `max_request_body_bytes` ↔ storage upload-size interaction. Page SHOULD recommend deploying storage on a separate server instance for very large uploads, OR using presigned PUT URLs to bypass the server-side body cap. Cross-link to #338 | _(open)_ | _(open)_ |
| FW-12 | Phase 03 / Cycle 2 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/339 | regression (information disclosure) | `features/file-storage` (list endpoint section) | `scripts/docs-test/bugs/file-storage.bug-5.sh` confirms `metadata::list` at `crates/fraiseql-storage/src/metadata/mod.rs:L166-L182` interpolates client-supplied `prefix` into a LIKE pattern via `format!("{pfx}%")` with no ESCAPE clause and no pre-bind `replace("%", ...)` defence. Client passes `prefix=%` or `prefix=%admin%` to enumerate the full bucket / scan for substring matches | Pending — page MUST describe `prefix` as a LIKE pattern (the actual behaviour) until fix lands, OR page MUST warn that PublicRead buckets are enumerable. Cross-link to #339 | _(open)_ | _(open)_ |
| FW-13 | Phase 03 / Cycle 3 (Writer-RED) | https://github.com/fraiseql/fraiseql/issues/340 | qol | `features/observers`, `operations/observer-runbook` | RED-evidence transcript `_internal/.plan/red-evidence/phase-03-cycle-03-stale-observer-toml.transcript` Observation 4 + static-source inspection of `crates/fraiseql-server/src/server/routing/observers.rs:L46-L62` — `observer_runtime_routes` mounted via `app.merge(...)` at the router ROOT (`/runtime/health`, `/runtime/reload`) instead of nested under `/api/observers/runtime/*` like the three other observer route groups | Pending — page MUST document `/runtime/health` (root) vs `/api/observers/{dlq,changelog,checkpoint}/*` (nested) layout verbatim and surface this as a known caveat. Cross-link to #340 | _(open)_ | _(open)_ |
| FW-14 | Phase 03 / Cycle 3 (Writer-RED) | https://github.com/fraiseql/fraiseql/issues/341 | regression | `operations/observer-runbook` (DLQ CLI workflows) | RED-evidence inspection of `crates/fraiseql-observers/src/cli/commands/dlq.rs:L38-L80` — DLQ `list / show / stats / retry / remove` subcommands return hard-coded mock JSON with observer IDs `obs-webhook` / `obs-email` / `obs-slack` baked into the source; `crates/fraiseql-cli/Cargo.toml` does NOT depend on `fraiseql-observers` so `fraiseql-cli` has no observer subcommand at all | Pending — runbook MUST replace `fraiseql-cli observer ...` invocations with HTTP API examples (`curl http://server/api/observers/dlq`), and add a `## Known issues` block pointing at #341 | _(open)_ | _(open)_ |
| FW-15 | Phase 03 / Cycle 3 (Writer-RED) | https://github.com/fraiseql/fraiseql/issues/342 | regression | `features/observers`, `building/observers`, `building/observer-webhook-patterns`, `operations/observer-runbook` (every TOML block) | RED-evidence transcript `_internal/.plan/red-evidence/phase-03-cycle-03-stale-observer-toml.transcript` Observations 1–3 — `fraiseql-cli`'s `ObserversConfig` validation schema has `backend`/`redis_url`/`nats_url`/`handlers` keys; `fraiseql-server`'s runtime `ObserverConfig` has `poll_interval_ms`/`batch_size`/`pool`. The two share no field. `ServerConfig` lacks `#[serde(deny_unknown_fields)]` (`server_config/mod.rs:L40-L42`) so the validation-side keys are silently swallowed by the server; runtime starts without registering any observer | Pending — all four pages MUST drop the `[[observers.handlers]]` array-of-tables shape and document the binary's actual `[observers]` schema. Observer definitions must be documented as DB-driven (`tb_observer`) via `POST /api/observers`. Cross-link to #342 | _(open)_ | _(open)_ |
| FW-16 | Phase 03 / Cycle 3 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/343 | regression (DoS amplifier) | `features/observers`, `operations/observer-runbook` | `scripts/docs-test/bugs/observers.bug-1.sh` confirms server's `ObserverConfig` (`server_config/observers.rs:L99-L130`) has no `max_dlq_size` field; the library's `ObserverRuntimeConfig` (`config/runtime.rs:L55-L66`) DOES, with a "drop newest + warn" contract; binary's `InMemoryDlq::push` (`runtime.rs:L687-L772`) is `Vec::push()` with no cap, no eviction, no metric. `mark_retry_failed` silently `items.retain()`-removes the item on failure | Pending — page MUST state the binary's DLQ is in-process, unbounded, and lost on restart; runbook MUST NOT promise `max_dlq_size` as an operator knob; recommend external monitoring on `dlq_count`. Cross-link to #343 | _(open)_ | _(open)_ |
| FW-17 | Phase 03 / Cycle 3 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/344 | regression (contract violation) | `features/observers`, `operations/observer-runbook` (DLQ retry section) | `scripts/docs-test/bugs/observers.bug-2.sh` confirms `dlq_retry_handler` and `dlq_retry_all_handler` (`dlq_handlers/mod.rs:L209-L350`) do `dlq.get(id)` → `executor.process_event(...)` → `dlq.remove(id)` with no atomic claim; `InMemoryDlq` exposes no `try_claim` primitive. Two concurrent retries on the same item BOTH dispatch the action — at-least-once contract slips to at-least-twice in the race | Pending — page MUST warn that concurrent `/dlq/{id}/retry` calls double-fire and recommend serialising retries (single-replica routing or operator-side lock); MUST NOT advise running `/dlq/retry-all` concurrently with per-item retries. Cross-link to #344 | _(open)_ | _(open)_ |
| FW-18 | Phase 03 / Cycle 3 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/345 | regression (security) | `features/observers`, `building/observer-webhook-patterns`, `operations/observer-runbook` | `scripts/docs-test/bugs/observers.bug-3.sh` confirms `WebhookAction::execute` (`actions.rs:L241-L300`) sends unsigned POSTs; whole-crate grep of `crates/fraiseql-observers/src/` returns zero hits for `hmac` / `signature` / `X-Webhook-Signature` / `X-FraiseQL-Signature`; the crate has no dependency on `hmac` (the `sha2` dep is for cache-key hashing only). Receivers cannot verify webhook origin | Pending — `/features/observers` MUST state "webhook payloads are NOT signed today (#345)"; `/building/observer-webhook-patterns` MUST recommend operator-side bearer secrets via `headers` map + reverse-proxy mTLS as the only verification path, AND document the plaintext-storage caveat. Cross-link to #345 | _(open)_ | _(open)_ |
| FW-19 | Phase 03 / Cycle 3 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/346 | regression (security / compliance) | `features/observers`, `operations/observer-runbook` (log-aggregation guidance) | `scripts/docs-test/bugs/observers.bug-4.sh` confirms four `info!` log lines per dispatch in `actions.rs:L254-L290` exposing the full webhook URL, the entire headers HashMap (including `Authorization` / `X-API-Key`), the body template, and the rendered JSON body. No redact / mask helper; no env-var or feature-flag suppression toggle | Pending — page MUST warn the default INFO log level emits the webhook URL, headers, and rendered body on every dispatch; MUST recommend `RUST_LOG=fraiseql_observers::actions=warn` in production; MUST advise against bearer secrets in the `headers` map without log filtering. Cross-link to #346 | _(open)_ | _(open)_ |
| FW-20 | Phase 03 / Cycle 3 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/347 | **security (critical)** | `features/observers`, `operations/observer-runbook` | `scripts/docs-test/bugs/observers.bug-5.sh` confirms `FRAISEQL_OBSERVERS_ALLOW_INSECURE=true` / `=1` triggers an early `return Ok(())` in nine independent guard sites across `actions.rs`, `executor/dispatch.rs`, and `ssrf.rs`; the bypass logs ONE warning via `INSECURE_WARN_ONCE.call_once`. No production-mode detector. Combined with FW-21 (anonymous observer creation), gives a one-step path to AWS metadata IAM-credential exfil via `http://169.254.169.254/latest/meta-data/...` | Pending — page MUST document this env var as the blast-radius switch it is; MUST recommend a deployment-process refuse-to-boot guard; MUST warn the toggle is one-shot-logged. Cross-link to #347, FW-21 (#348), FW-19 (#346) | _(open)_ | _(open)_ |
| FW-21 | Phase 03 / Cycle 3 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/348 | **security (critical)** | `features/observers`, `building/observers`, `operations/observer-runbook` | `scripts/docs-test/bugs/observers.bug-6.sh` confirms `create_observer`/`update_observer` use `OptionalSecurityContext` (no required-auth gate); `delete_observer`/`enable_observer`/`disable_observer`/`reload_observers`/`get_runtime_health` have NO auth extractor at all; ALL DLQ handlers (`delivery_health`/`dlq_list`/`dlq_get`/`dlq_retry`/`dlq_retry_all`) have NO auth extractor; routes layer applies no auth middleware. Anonymous attacker installs an exfil observer, silently re-routes existing observers, triggers DoS-via-reload, replays DLQ against attacker URL, and reads bearer-token secrets from observer GET responses | Pending — page MUST warn the observer admin API is unauthenticated and MUST NOT be exposed to the public internet; MUST recommend reverse-proxy auth (mTLS or bearer-token gate) in front of `/api/observers`, `/api/observers/dlq`, `/runtime/*`; MUST cross-link FW-13 (root-mount makes `/runtime/*` harder to gate). Cross-link to #348 | _(open)_ | _(open)_ |
| FW-22 | Phase 03 / Cycle 3 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/349 | regression (correctness) | `features/observers` (action catalogue), `building/observers` | `scripts/docs-test/bugs/observers.bug-7.sh` confirms `EmailAction::execute` (`actions.rs:L484-L505`) is a stub — every parameter `_`-prefixed (unused), body says "Stub implementation", returns `Ok(EmailResponse { success: true, message_id: Some(uuid::Uuid::new_v4()), duration_ms: 10.0 })`. Crate has no `lettre`/SMTP-client dependency. Action records success, metrics tick, DLQ never sees the item — operators cannot detect the silent failure | Pending — page MUST NOT include Email-action examples that suggest delivery happens; cross-link this issue from the Email row and direct readers to the Webhook action against their transactional-email provider's HTTP API. Cross-link to #349 | _(open)_ | _(open)_ |
| FW-23 | Phase 03 / Cycle 3 (Bug-Finder) | https://github.com/fraiseql/fraiseql/issues/350 | regression (library-vs-binary class) | `features/observers`, `operations/observer-runbook` | `scripts/docs-test/bugs/observers.bug-8.sh` confirms server's `ObserverConfig` has no `transport` field; `init_observer_runtime` (`extensions.rs:L362-L389`) constructs a server-local `ObserverRuntimeConfig` (NOT the library type) with `.with_poll_interval()` only — no `TransportConfig`, no `.with_env_overrides()`. `ObserverRuntime::start` (`runtime.rs:L302-L304`) unconditionally creates a `ChangeLogListener` (PG LISTEN/NOTIFY). `FRAISEQL_OBSERVER_TRANSPORT` is read ONLY in `crates/fraiseql-observers/`, never in the server crate. `observers-nats` Cargo feature changes nothing at the binary's runtime wiring | Pending — page MUST state the off-the-shelf binary supports only PG LISTEN/NOTIFY transport; MUST NOT advertise `[observers] backend = "nats"` or `FRAISEQL_OBSERVER_TRANSPORT=nats` as binary knobs; library-API path (host binary calls `ObserverRuntimeConfig::with_env_overrides()`) needed for NATS / InMemory. Same class as FW-3 / FW-7 / FW-15 — cross-link all four | _(open)_ | _(open)_ |
| FW-24 | Phase 03 / Cycle 4 (Writer-RED) | https://github.com/fraiseql/fraiseql/issues/356 | regression (security control silently absent) | `building/authentication` (brute-force protection section) | RED-evidence transcript `_internal/.plan/red-evidence/phase-03-cycle-04-jwt-auth-flow.transcript` Observation 7 — CLI TOML schema `RateLimitingSecurityConfig` (`crates/fraiseql-cli/src/config/toml_schema/security.rs:L181-L249`) accepts `failed_login_max_attempts` / `failed_login_lockout_secs`; server runtime mirror (`crates/fraiseql-server/src/middleware/rate_limit/config.rs:L7-L52`) has NO `failed_login_*` fields. Serde silently drops the keys; whole-server grep for `failed_logins` / `AuthRateLimiter` in `crates/fraiseql-server/src/` returns zero hits. `fraiseql-auth::AuthRateLimiters.failed_logins` exists but is never instantiated by the server | Pending — page MUST NOT promise brute-force protection via `[security.rate_limiting]` against the off-the-shelf binary at this SHA. Page MUST recommend edge-side rate limiting (nginx, Cloudflare) or library-API host wrapper. Same class as FW-15 (#342) — cross-link. Cross-link to #356 | _(open)_ | _(open)_ |
| FW-25 | Phase 03 / Cycle 4 (Writer-RED) | https://github.com/fraiseql/fraiseql/issues/357 | regression (CLI schema split) | `building/authentication` (token-revocation backends section) | RED-evidence transcript `_internal/.plan/red-evidence/phase-03-cycle-04-jwt-auth-flow.transcript` Observation 8 — CLI TOML schema documents `backend = "redis" \| "postgres" \| "memory"` (`crates/fraiseql-cli/src/config/toml_schema/security.rs:L466-L501`); server `revocation_manager_from_schema` (`crates/fraiseql-server/src/token_revocation.rs:L399-L437`) match arms cover `memory` and `redis` only — `postgres` lands in the "Unknown revocation backend" warn-then-fallback arm. CLI-validated `backend = "postgres"` silently downgrades to in-process in-memory (lost on restart, no cross-replica revoke) | Pending — page MUST document only `memory` and `redis` as live revocation backends at this SHA. Page MUST warn that `backend = "postgres"` silently falls back to in-memory and recommend library-API path or wait-for-fix. Same class as FW-15 / FW-24 — cross-link. Cross-link to #357 | _(open)_ | _(open)_ |

---

## Phase 09 triage threshold (G3)

The severity threshold above which phase 09 must close all items before phase 10 starts is **human gate G3**. Default proposal (subject to G3 confirmation): **blocker** and **regression** must be closed-by-fix or closed-as-wrong. **qol** may remain open as accepted-limitation with a `## Known issues` block on the affected page.

When phase 09 opens, the Bug-Finder persona writes a G3 proposal to `handoff.md` and stops for human input.
